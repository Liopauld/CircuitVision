# Base URL of the CircuitVision API.
# Web dev: localhost is fine.
VITE_API_URL=http://localhost:4000/api

# For a real mobile device (Capacitor), localhost won't resolve — use your
# machine's LAN IP or a deployed API, e.g.:
# VITE_API_URL=http://192.168.1.10:4000/api
